// TOMBSTONE — not rendered anywhere; safe to delete this file
// (src/components/home/feature-poster.tsx).
//
// FeaturePosters rendered the two invented "matchday poster" panels
// (HOME_FEATURES in src/data/club.ts) that used to sit under the home page
// hero — fabricated marketing copy, not the club's real content, so both the
// data and this component were removed when the home page was rebuilt to
// stack the live Next Match and Latest News sections instead.
//
// Reduced to an empty module rather than deleted, since this device bridge
// has no delete capability.
export {};
