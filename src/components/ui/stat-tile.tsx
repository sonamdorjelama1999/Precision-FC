/**
 * TOMBSTONE — safe to delete this file.
 *
 * A generic stat-card primitive with no importer anywhere in the project —
 * confirmed unused, not tied to the legacy match-log removal like its
 * neighbors in this cleanup pass.
 *
 * Kept as an empty module rather than removed outright because this device
 * bridge can write file contents but can't delete files — deleting
 * src/components/ui/stat-tile.tsx by hand is the real cleanup.
 */
export {};
