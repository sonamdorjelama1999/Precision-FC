/**
 * TOMBSTONE — safe to delete this file.
 *
 * Rendered lib/stats.ts's scorerTable() against the legacy Fixture/GoalEvent
 * match-log, which no longer exists in prisma/schema.prisma. Nothing has
 * imported this component since the fixtures section was rebuilt on the
 * Team/Match model — goal-by-goal stats never migrated over (see the module
 * comment in features/matches/queries.ts for how that could attach later).
 *
 * Kept as an empty module rather than removed outright because this device
 * bridge can write file contents but can't delete files — deleting
 * src/components/players/scorer-table.tsx by hand is the real cleanup.
 */
export {};
